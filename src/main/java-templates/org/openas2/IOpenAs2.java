package org.openas2;

/**
 * Interface which contains the current open AS2 version.
 * 
 * @author PÉRIÉ Fabien
 */
public interface IOpenAs2
{
	static final String NAME = "${project.groupId}:${project.artifactId}";

	static final String CURRENT_VERSION = "${project.version}";
}
